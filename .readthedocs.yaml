version: 2

python:
  version: "3.8"
  # Install our python package before building the docs
  install:
    - method: pip
      path: .
    - method: pip
      extra_requirements:
        - sphinxcontrib-images

sphinx:
  fail_on_warning: true

formats:
  - pdf
  - epub
